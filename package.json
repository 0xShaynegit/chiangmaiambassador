{
  "name": "chiangmaiambassador",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "index": "pagefind --site . --output-path pagefind",
    "build": "npm run index"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
